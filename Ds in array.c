#include<stdio.h>
#include<conio.h>
void main(){
    int arr[N],key=40;
    arr[N]={10,20,40,50}
    scanf("%d",&arr[N]);
    while(arr[N]!=key){
      arr[n]=next;
      next=arr[n].next;  // while searching that position
    }
    if(arr[N]==key){
        arr[2]=null;    //Second position of '40' will be removed
    }
    return 0;
}