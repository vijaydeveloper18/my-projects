var arr=[3];                 //To create the array
var front=0;rear=arr[0];
for(var i=0;i<=3;i++){      //To check that condition
    arr[i]=val;            //To insert data into array
    rear++;
}
if(arr[lastindexof(3)]!==null && rear==arr[lastindexof(3)]){  //Data fully stored
    console.log("The queue is full");
}
if(rear!=front){
    front++;
   for(arr[i]=3;arr[i]>=0;arr[i]--){
    arr[i]=null;                      //Delete the datas in array
}
}else{
    console.log("The queue is empty");
}

